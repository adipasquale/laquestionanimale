---
layout: layouts/base.njk
---
<h1>{{ title }}</h1>

<div class="post-metadata" style="font-style: italic;">
	article mis à jour le <time datetime="{{ page.date | htmlDateString }}">{{ page.date | readableDate }}</time>
	{# {%- for tag in tags | filterTagList %} #}
	{# {%- set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
		<li><a href="{{ tagUrl }}" class="post-tag">{{ tag }}</a>{%- if not loop.last %}, {% endif %}</li>
	{%- endfor %} #}
</div>

{{ content | safe }}

{# {%- if collections.articles %}
{%- set previousPost = collections.articles | getPreviousCollectionItem %}
{%- set nextPost = collections.articles | getNextCollectionItem %}
{%- if nextPost or previousPost %}
<ul class="links-nextprev">
	{%- if previousPost %}<li>Précédent: <a href="{{ previousPost.url }}">{{ previousPost.data.title }}</a></li>{% endif %}
	{%- if nextPost %}<li>Suivant: <a href="{{ nextPost.url }}">{{ nextPost.data.title }}</a></li>{% endif %}
</ul>
{%- endif %}
{%- endif %} #}
